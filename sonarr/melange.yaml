package:
  name: sonarr
  version: 4.0.17.2952 # renovate: datasource=github-releases depName=Sonarr/Sonarr
  epoch: 2
  description: TV show collection manager for Usenet and BitTorrent users
  copyright:
    - license: GPL-3.0
  options:
    no-depends: true

vars:
  dotnet-sdk-version: "10"

environment:
  environment:
    DOTNET_CLI_TELEMETRY_OPTOUT: "1"
    SONARR_VERSION: "${{package.version}}"
    BRANCH: "v${{package.version}}"
  contents:
    repositories:
      - &wolfi-repository https://packages.wolfi.dev/os
    keyring:
      - &wolfi-keyring https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - bash
      - build-base
      - busybox
      - dotnet-10-sdk
      - nodejs
      - pnpm
      - yarn

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Sonarr/Sonarr
      tag: v${{package.version}}

  - runs: |
      echo '{"sdk":{"version":"${{vars.dotnet-sdk-version}}"}}' > global.json
      sed -i 's#<TreatWarningsAsErrors>true</TreatWarningsAsErrors>#<TreatWarningsAsErrors>false</TreatWarningsAsErrors>#' src/Directory.Build.props

  - runs: |
      case "${{build.arch}}" in
        x86_64)  rid="x64" ;;
        aarch64) rid="arm64" ;;
      esac

      ./build.sh \
        --backend \
        --frontend \
        --packages \
        -f net6.0 \
        -r "linux-${rid}"

  - runs: |
      mkdir -p "${{targets.destdir}}/opt/sonarr"
      rm -f _output/net*/linux-*/publish/ServiceInstall*
      rm -f _output/net*/linux-*/publish/ServiceUninstall*
      rm -f _output/net*/linux-*/publish/Sonarr.Update*
      rm -f _output/net*/linux-*/publish/Sonarr.Windows*
      cp -af _output/net*/linux-*/publish/. "${{targets.destdir}}/opt/sonarr/"
      cp -af _output/UI "${{targets.destdir}}/opt/sonarr/"

  - runs: |
      chmod -R 0755 "${{targets.destdir}}"/opt/sonarr/*

      mkdir -p "${{targets.destdir}}/usr/bin"
      ln -sf /opt/sonarr/Sonarr "${{targets.destdir}}/usr/bin/sonarr"

  - uses: strip

update:
  enabled: true
  github:
    identifier: Sonarr/Sonarr
    use-tag: true
    strip-prefix: v

test:
  environment:
    contents:
      repositories:
        - *wolfi-repository
      keyring:
        - *wolfi-keyring
      packages:
        - busybox
        - ${{package.name}}=${{package.full-version}}
  pipeline:
    - name: Basic Sonarr package test
      runs: |
        test -x /opt/sonarr/Sonarr
        test -L /usr/bin/sonarr
        test -d /opt/sonarr/UI
