package:
  name: tautulli
  version: 2.17.0
  epoch: 1
  description: Monitoring, analytics, and notifications for Plex Media Server
  copyright:
    - license: GPL-3.0-or-later
  options:
    no-depends: true

environment:
  contents:
    repositories:
      - &wolfi-repository https://packages.wolfi.dev/os
    keyring:
      - &wolfi-keyring https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - python3

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Tautulli/Tautulli
      tag: v${{package.version}}

  - runs: |
      mkdir -p \
        "${{targets.destdir}}/config" \
        "${{targets.destdir}}/tautulli" \
        "${{targets.destdir}}/tautulli/etc" \
        "${{targets.destdir}}/tautulli/var" \
        "${{targets.destdir}}/opt/tautulli"

  - runs: |
      cp -a \
        data \
        lib \
        plexpy \
        PlexPy.py \
        Tautulli.py \
        "${{targets.destdir}}/opt/tautulli/"

  - runs: |
      printf '%s\n' master > "${{targets.destdir}}/opt/tautulli/branch.txt"
      git rev-parse HEAD > "${{targets.destdir}}/opt/tautulli/version.txt"

  - runs: |
      cp -a rootfs/. "${{targets.destdir}}/"

  - runs: |
      : > "${{targets.destdir}}/config/DOCKER"

update:
  enabled: true
  github:
    identifier: Tautulli/Tautulli
    use-tag: true
    strip-prefix: v

test:
  environment:
    contents:
      repositories:
        - *wolfi-repository
      keyring:
        - *wolfi-keyring
      packages:
        - busybox
        - python3
        - ${{package.name}}=${{package.full-version}}
  pipeline:
    - name: Basic Tautulli package test
      runs: |
        test -f /opt/tautulli/Tautulli.py
        test -d /opt/tautulli/lib
        test -f /opt/tautulli/version.txt
        test -f /config/DOCKER
        python3 /opt/tautulli/Tautulli.py --help >/dev/null
